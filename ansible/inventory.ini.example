[primary]
PRIMARY_PUBLIC_IP ansible_user=ubuntu ansible_ssh_private_key_file=/path/to/primary.pem

[standby]
STANDBY_PUBLIC_IP ansible_user=ubuntu ansible_ssh_private_key_file=/path/to/standby.pem

[app:children]
primary
standby
